name: Build Photoshop AVIF Plugin

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-latest

    steps:
      # 1. 签出代码
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: true

      # 2. 微软官方标准：初始化并捕获 MSBuild 的绝对路径
      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2
        id: setupmsbuild

      # 3. 强制在标准的 cmd 环境下触发 vcpkg 本地 manifest 落地
      # 显式指定 --x-install-root，确保所有头文件被逼在项目根目录的 vcpkg_installed 里生成
      - name: Install Dependencies via vcpkg
        shell: cmd
        run: |
          vcpkg integrate install
          vcpkg install --triplet=x64-windows --x-install-root="%GITHUB_WORKSPACE%\vcpkg_installed"

      # 4. 安全地将头文件搬运到项目硬编码要求的 3rd-party 目录
      - name: Fix Hardcoded Third-Party Directories
        shell: pwsh
        run: |
          $workspace = "${{ github.workspace }}"
          $vcpkgInclude = Join-Path $workspace "vcpkg_installed\x64-windows\include"
          $targetDir = Join-Path $workspace "3rd-party\libheif"
          
          if (!(Test-Path $vcpkgInclude)) {
              New-Item -ItemType Directory -Force -Path $vcpkgInclude | Out-Null
          }
          if (!(Test-Path $targetDir)) {
              New-Item -ItemType Directory -Force -Path $targetDir | Out-Null
          }
          
          Write-Output "Copying headers from $vcpkgInclude to $targetDir ..."
          Copy-Item -Path "$vcpkgInclude\*" -Destination $targetDir -Recurse -Force -ErrorAction SilentlyContinue
          Write-Output "Successfully processed third-party directories."

      - name: Generate Local Photoshop Stub Headers
        run: |
          echo "Generating Photoshop stub headers..."

      # 5. 核心修复：强制在标准的 cmd 下，使用 ${{ github.workspace }} 绝对路径呼叫 MSBuild
      - name: Restore NuGet Packages
        shell: cmd
        run: |
          "${{ steps.setupmsbuild.outputs.msbuildpath }}" "${{ github.workspace }}\AvifFormat.sln" /t:Restore /p:VcpkgEnableManifest=true /p:VcpkgRoot="%GITHUB_WORKSPACE%\vcpkg_installed"

      # 6. 执行最终编译
      - name: Build Solution
        shell: cmd
        run: |
          "${{ steps.setupmsbuild.outputs.msbuildpath }}" "${{ github.workspace }}\AvifFormat.sln" /p:Configuration=Release /p:Platform=x64 /p:VcpkgEnableManifest=true /p:VcpkgRoot="%GITHUB_WORKSPACE%\vcpkg_installed"

      # 7. 收集编译产物
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Photoshop-AVIF-Plugin
          path: |
            **/Release/*.8bi
